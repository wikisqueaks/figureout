# List Available Journals

Returns a data frame of journal names for which default figure
dimensions are available.

## Usage

``` r
show_journals()
```

## Value

A data frame with one column, `Journals`, containing sorted journal
names.
